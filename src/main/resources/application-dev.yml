spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/bleustudio_db
    username: root
    password: aa17410zz

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true